[tool.ruff]
select = ["E", "F", "I"]
fixable = ["ALL"]
exclude = [
    ".git",
    ".ruff_cache",
    ".venv",
    "venv",
    "__pycache__",
    "dist",
]
line-length = 100
target-version = "py312"

[tool.ruff.isort]
known-first-party = ["src"]
